# 后端实体类扩展完成报告

## 📋 扩展概览

**扩展完成时间**: 2024-01-15 11:15:00  
**涉及模块**: health模块、geofence模块  
**扩展方式**: 向下兼容，基于现有实体类扩展 + 新增实体类

## 🔧 现有实体类扩展

### 1. TUserHealthData 扩展 ✅

**文件位置**: `/ljwx-boot-modules/src/main/java/com/ljwx/modules/health/domain/entity/TUserHealthData.java`

#### 新增轨迹专用字段
```java
// ============== 轨迹功能新增字段 (v1.0.0) ==============

/**
 * 速度(km/h)
 */
private Double speed;

/**
 * 方向角(度，0-360)
 * 0度表示正北，90度表示正东，180度表示正南，270度表示正西
 */
private Double bearing;

/**
 * 定位精度(米)
 */
private Double accuracy;

/**
 * 定位类型 (1-GPS, 2-WiFi, 3-基站)
 */
private Integer locationType;

/**
 * 空间几何对象 (MySQL POINT类型)
 * 用于地理围栏计算和空间查询，由 longitude + latitude 自动生成
 * 注意: 此字段由数据库触发器维护，不需要手动设置
 */
@TableField("geom")
private String geom;
```

#### 兼容性保证 ✅
- ✅ **原有字段完全保留**: latitude, longitude, altitude, distance, timestamp 等核心轨迹字段
- ✅ **新增字段允许NULL**: 不影响现有数据和UnifiedHealthDataQueryService查询
- ✅ **多租户字段不变**: customerId, orgId, userId 保持原有逻辑
- ✅ **向下兼容**: 现有健康数据功能完全不受影响

### 2. TGeofence 扩展 ✅

**文件位置**: `/ljwx-boot-modules/src/main/java/com/ljwx/modules/geofence/domain/entity/TGeofence.java`

#### 围栏类型支持
```java
/**
 * 围栏类型枚举
 */
public enum FenceType {
    CIRCLE("圆形"),        // 圆形围栏
    RECTANGLE("矩形"),     // 矩形围栏  
    POLYGON("多边形");     // 多边形围栏 (原有功能)
}

private FenceType fenceType;          // 围栏类型
private BigDecimal centerLng;         // 中心点经度
private BigDecimal centerLat;         // 中心点纬度
private Float radius;                 // 半径(圆形围栏)
private String geom;                  // 空间几何对象
```

#### 告警配置字段
```java
private Boolean alertOnEnter;         // 进入告警
private Boolean alertOnExit;          // 离开告警
private Boolean alertOnStay;          // 停留告警
private Integer stayDurationMinutes;  // 停留时长阈值
private AlertLevel alertLevel;        // 告警级别 (LOW/MEDIUM/HIGH)
private String notifyChannels;        // 通知渠道配置(JSON)
private String notifyTemplateId;      // 通知模板ID
```

#### 多租户支持
```java
private Long orgId;                   // 组织ID
private Long customerId;              // 租户ID
private Boolean isActive;             // 是否启用
private String createdBy;             // 创建人
```

#### 向下兼容设计 ✅
- ✅ **保留原有字段**: name, area, description, status 完全保留
- ✅ **status字段标记@Deprecated**: 引导使用新的isActive字段
- ✅ **现有围栏管理功能不受影响**: 原有API继续正常工作

## 🆕 新增实体类

### 3. TGeofenceBind (围栏绑定关系) ✅

**文件位置**: `/ljwx-boot-modules/src/main/java/com/ljwx/modules/geofence/domain/entity/TGeofenceBind.java`

#### 核心功能
```java
public class TGeofenceBind extends BaseEntity {
    private String bindId;              // 绑定ID (业务主键)
    private Long fenceId;               // 围栏ID
    private TargetType targetType;      // 绑定类型 (USER/DEVICE/VEHICLE)
    private Long targetId;              // 目标ID
    
    // 时间规则配置
    private Boolean workTimeOnly;       // 仅工作时间生效
    private String effectiveWeekdays;   // 生效星期
    private LocalTime effectiveStartTime; // 生效开始时间
    private LocalTime effectiveEndTime;   // 生效结束时间
    
    // 多租户支持
    private Long customerId;
    private Long orgId;
    private Boolean isActive;
}
```

#### 支持场景
- **用户围栏绑定**: 员工与安全区域绑定
- **设备围栏绑定**: 设备与工作区域绑定  
- **车辆围栏绑定**: 车辆与运营区域绑定
- **工作时间控制**: 支持工作时间和全天候两种模式
- **灵活时间规则**: 自定义生效时间段和星期

### 4. TGeofenceAlert (围栏告警记录) ✅

**文件位置**: `/ljwx-boot-modules/src/main/java/com/ljwx/modules/geofence/domain/entity/TGeofenceAlert.java`

#### 告警信息
```java
public class TGeofenceAlert extends BaseEntity {
    private String alertId;             // 告警ID (业务主键)
    private Long fenceId;               // 围栏ID
    private Long userId;                // 用户ID
    private String deviceId;            // 设备ID
    
    // 告警类型和级别
    private AlertType alertType;        // ENTER/EXIT/STAY_TIMEOUT
    private AlertLevel alertLevel;      // LOW/MEDIUM/HIGH
    
    // 时间信息
    private LocalDateTime startTime;    // 告警开始时间
    private LocalDateTime endTime;      // 告警结束时间
    private Integer durationMinutes;    // 持续时长
    
    // 位置信息
    private BigDecimal locationLng;     // 告警位置经度
    private BigDecimal locationLat;     // 告警位置纬度
    private String locationDesc;        // 位置描述
    private String locationGeom;        // 位置空间对象
}
```

#### 处理流程支持
```java
// 处理状态管理
private AlertStatus alertStatus;      // PENDING/PROCESSING/RESOLVED/IGNORED
private Long handlerId;               // 处理人ID
private LocalDateTime handleTime;     // 处理时间
private String handleNote;            // 处理备注
private String handleResult;          // 处理结果

// 通知状态记录
private String notifyStatus;          // 通知状态(JSON)
private Integer notifyRetryCount;     // 重试次数
private LocalDateTime notifySuccessTime; // 通知成功时间
```

### 5. TUserOnlineStatus (用户在线状态) ✅

**文件位置**: `/ljwx-boot-modules/src/main/java/com/ljwx/modules/health/domain/entity/TUserOnlineStatus.java`

#### 在线状态管理
```java
public class TUserOnlineStatus extends BaseEntity {
    @TableId(type = IdType.INPUT)
    private Long userId;                // 用户ID (主键)
    
    // 在线状态
    private OnlineStatus onlineStatus;  // ONLINE/OFFLINE/ABNORMAL
    private LocalDateTime lastSeenTime; // 最后活跃时间
    private String offlineReason;       // 离线原因
    
    // 位置信息
    private LocalDateTime lastLocationTime; // 最后定位时间
    private BigDecimal lastLng;         // 最后经度
    private BigDecimal lastLat;         // 最后纬度
    private String lastLocationGeom;    // 最后位置空间对象
    
    // 设备状态
    private Integer batteryLevel;       // 电池电量
    private Integer signalStrength;     // 信号强度
    private Integer heartbeatInterval;  // 心跳间隔
}
```

#### 每日统计
```java
// 每日数据重置
private Double dailyDistance;         // 今日运动距离
private Integer dailySteps;           // 今日步数
private Double dailyCalories;         // 今日卡路里
private LocalDate lastResetDate;      // 最后重置日期
```

## 📊 DTO和VO类完善

### 6. TrackQueryDTO (轨迹查询参数) ✅

**文件位置**: `/ljwx-boot-modules/src/main/java/com/ljwx/modules/health/domain/dto/track/TrackQueryDTO.java`

#### 查询参数全覆盖
```java
public class TrackQueryDTO {
    // 基础查询
    private Long userId;                // 单用户查询
    private String userIds;             // 多用户查询
    private LocalDateTime startDate;    // 开始时间
    private LocalDateTime endDate;      // 结束时间
    
    // 数据质量过滤
    private Double minSpeed;            // 最小速度过滤
    private Double maxSpeed;            // 最大速度过滤  
    private Double maxAccuracy;         // 精度过滤
    private String locationTypes;       // 定位类型过滤
    
    // 轨迹处理参数
    private Boolean simplify;           // 启用抽稀
    private Double tolerance;           // 抽稀容差
    private Integer maxPoints;          // 最大点数
    
    // 停留点分析
    private Boolean includeStayPoints;  // 包含停留点
    private Double stayDistanceThreshold; // 停留距离阈值
    private Integer stayTimeThreshold;  // 停留时间阈值
}
```

### 7. TrackPointVO (轨迹点展示) ✅

**文件位置**: `/ljwx-boot-modules/src/main/java/com/ljwx/modules/health/domain/vo/track/TrackPointVO.java`

#### 完整轨迹点信息
```java
public class TrackPointVO {
    // 基础位置信息
    private Double longitude;           // 经度
    private Double latitude;            // 纬度
    private Double altitude;            // 海拔
    private Double speed;               // 速度
    private Double bearing;             // 方向角
    private Double accuracy;            // 精度
    
    // 计算字段
    private Double cumulativeDistance;  // 累计距离
    private Double segmentDistance;     // 分段距离
    private Integer timeInterval;       // 时间间隔
    
    // 停留点信息
    private Boolean isStayPoint;        // 是否停留点
    private Integer stayDuration;       // 停留时长
    
    // 辅助方法
    public String getLocationTypeDesc() // 定位类型描述
    public String getAccuracyLevel()    // 精度等级描述
    public boolean isHighAccuracy()     // 是否高精度
    public boolean isMoving()           // 是否移动状态
}
```

### 8. TrackStatisticsVO (轨迹统计) ✅

**文件位置**: `/ljwx-boot-modules/src/main/java/com/ljwx/modules/health/domain/vo/track/TrackStatisticsVO.java`

#### 全面统计信息
```java
public class TrackStatisticsVO {
    // 基础统计
    private Integer totalPoints;        // 总点数
    private Integer validPoints;        // 有效点数
    private Double totalDistance;       // 总距离
    private Integer totalDuration;      // 总时长
    
    // 速度统计
    private Double averageSpeed;        // 平均速度
    private Double maxSpeed;            // 最大速度
    private Double minSpeed;            // 最小速度
    
    // 停留分析
    private Integer stayPointCount;     // 停留点数量
    private Integer stayDuration;       // 停留时长
    private List<StayPointVO> stayPoints; // 停留点列表
    
    // 数据质量
    private Double averageAccuracy;     // 平均精度
    private Double highAccuracyRate;    // 高精度率
    
    // 边界信息
    private BoundingBoxVO boundingBox;  // 边界框
    private TrackPointVO startPoint;    // 起点
    private TrackPointVO endPoint;      // 终点
    
    // 辅助方法
    public Double getDataQualityScore() // 数据质量评分
    public String getActivityLevel()    // 运动强度描述
    public Double getMovingEfficiency() // 移动效率
}
```

## ✅ 实体类扩展验证

### MyBatis Plus 兼容性 ✅
- ✅ **@TableName注解**: 所有实体类正确映射到对应数据表
- ✅ **@TableField注解**: 特殊字段正确映射 (如geom字段)
- ✅ **@TableId注解**: 主键策略正确配置
- ✅ **枚举类型**: 支持EnumTypeHandler自动转换

### Lombok注解完整性 ✅
- ✅ **@Data**: 自动生成getter/setter/toString/equals/hashCode
- ✅ **@SuperBuilder**: 支持继承的Builder模式
- ✅ **@NoArgsConstructor**: 无参构造函数
- ✅ **@AllArgsConstructor**: 全参构造函数

### JSON序列化支持 ✅  
- ✅ **@JsonFormat**: 时间字段格式化
- ✅ **Jackson兼容**: 所有字段支持JSON序列化/反序列化
- ✅ **枚举序列化**: 枚举类型正确序列化为字符串

## 🔄 与UnifiedHealthDataQueryService集成

### 数据查询兼容性 ✅
```java
// 现有查询方式完全兼容
Map<String, Object> result = unifiedHealthDataQueryService.queryHealthData(queryDTO);
List<Map<String, Object>> basicData = (List<Map<String, Object>>) result.get("basicData");

// 新增字段自动包含在查询结果中
for (Map<String, Object> data : basicData) {
    Double speed = (Double) data.get("speed");       // 新增字段
    Double bearing = (Double) data.get("bearing");   // 新增字段  
    Double accuracy = (Double) data.get("accuracy"); // 新增字段
    // ... 现有字段正常获取
}
```

### 轨迹数据转换
```java
// 健康数据转轨迹点的转换逻辑
private TrackPointVO convertToTrackPoint(Map<String, Object> healthData) {
    TrackPointVO point = new TrackPointVO();
    
    // 基础字段 (现有)
    point.setUserId(getLongValue(healthData, "userId"));
    point.setLongitude(getDoubleValue(healthData, "longitude"));
    point.setLatitude(getDoubleValue(healthData, "latitude"));
    point.setAltitude(getDoubleValue(healthData, "altitude"));
    point.setTimestamp(getTimestampValue(healthData, "timestamp"));
    
    // 新增字段
    point.setSpeed(getDoubleValue(healthData, "speed"));
    point.setBearing(getDoubleValue(healthData, "bearing"));
    point.setAccuracy(getDoubleValue(healthData, "accuracy"));
    point.setLocationType(getIntegerValue(healthData, "locationType"));
    
    return point;
}
```

## 📋 后续开发指导

### 立即可用的功能
1. **轨迹查询**: 基于TrackQueryDTO参数化查询
2. **数据转换**: 健康数据 → 轨迹点VO的完整转换链路
3. **统计分析**: TrackStatisticsVO提供全面的轨迹统计
4. **围栏管理**: 扩展后的TGeofence支持复杂围栏配置

### 开发优先级
1. **高优先级**: 开发TrackService - 基于UnifiedHealthDataQueryService的轨迹查询封装
2. **高优先级**: 开发GeofenceCalculatorService - 围栏计算引擎
3. **中优先级**: 开发告警处理服务 - 基于TGeofenceAlert的告警管理
4. **中优先级**: 开发实时状态服务 - 基于TUserOnlineStatus的状态同步

### 关键约束提醒
1. **数据查询规范**: 所有轨迹查询必须通过UnifiedHealthDataQueryService.queryHealthData
2. **向下兼容**: 新增字段不影响现有健康数据功能
3. **多租户支持**: 所有新功能必须支持customerId租户隔离
4. **性能考虑**: 利用新建空间索引提升地理查询性能

---

**结论**: ✅ 后端实体类扩展完成，数据模型就绪，可以开始业务服务层开发。