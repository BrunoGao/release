# 灵境万象(LJWX)系统离线部署完整指南

*最后更新: 2024-12-20*

## 概述

本指南提供完整的离线部署方案，客户可在有网络环境下载载Docker镜像，然后在无网络的服务器环境中进行离线部署。

---

## 第一阶段：镜像下载准备（有网络环境）

### 1. 环境准备

#### 1.1 系统要求
- **操作系统**: Windows 10/11, macOS 10.15+, Ubuntu 18.04+, CentOS 7+
- **Docker版本**: 18.06+
- **硬盘空间**: 至少5GB可用空间
- **网络**: 稳定的互联网连接

#### 1.2 安装Docker
```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# CentOS/RHEL
sudo yum install -y docker-ce docker-ce-cli containerd.io
sudo systemctl start docker
sudo systemctl enable docker

# Windows/macOS: 安装Docker Desktop
```

### 2. 镜像下载脚本

#### 2.1 创建下载脚本
```bash
# 创建下载目录
mkdir -p ljwx-offline && cd ljwx-offline

# 创建镜像下载脚本
cat > download-images.sh << 'EOF'
#!/bin/bash
# LJWX离线镜像下载脚本

set -e #出错即停

# 配置变量
REGISTRY="crpi-yilnm6upy4pmbp67.cn-shenzhen.personal.cr.aliyuncs.com"
VERSION="1.2.14" #版本号
REDIS_VERSION="1.1.7" #Redis版本

# 镜像列表
declare -a IMAGES=(
    "$REGISTRY/ljwx/ljwx-mysql:$VERSION"
    "$REGISTRY/ljwx/ljwx-redis:$REDIS_VERSION"
    "$REGISTRY/ljwx/ljwx-boot:$VERSION"
    "$REGISTRY/ljwx/ljwx-bigscreen:$VERSION"
    "$REGISTRY/ljwx/ljwx-admin:$VERSION"
)

echo "🐳 开始下载LJWX Docker镜像..."
echo "📦 总计 ${#IMAGES[@]} 个镜像"

# 下载镜像
for image in "${IMAGES[@]}"; do
    echo "⬇️  下载镜像: $image"
    docker pull "$image" || { echo "❌ 下载失败: $image"; exit 1; }
done

echo "💾 开始导出镜像..."
# 导出所有镜像为tar文件
docker save "${IMAGES[@]}" -o ljwx-images-${VERSION}.tar

# 压缩镜像文件
echo "🗜️  压缩镜像文件..."
gzip ljwx-images-${VERSION}.tar

echo "✅ 镜像下载完成!"
echo "📁 生成文件: ljwx-images-${VERSION}.tar.gz"
echo "📊 文件大小: $(du -h ljwx-images-${VERSION}.tar.gz | cut -f1)"
EOF

chmod +x download-images.sh
```

#### 2.2 执行下载
```bash
# 执行下载脚本
./download-images.sh

# 验证下载结果
ls -lh ljwx-images-*.tar.gz
```

### 3. 下载部署文件

#### 3.1 下载部署包
```bash
# 方式1: 从代码仓库下载(如果有访问权限)
git clone <仓库地址> ljwx-deployment
cd ljwx-deployment/docker/client-deployment

# 方式2: 手动下载关键文件
mkdir -p ljwx-deployment
cd ljwx-deployment

# 下载核心文件(需要替换为实际下载地址)
wget <服务器地址>/docker-compose.yml
wget <服务器地址>/deploy-client.sh
wget <服务器地址>/reset-database.sh
wget <服务器地址>/custom-config.env
wget <服务器地址>/client-data.sql
wget <服务器地址>/wait-for-it.sh
```

#### 3.2 创建离线部署包
```bash
# 返回上级目录
cd ..

# 创建完整离线包
tar -czf ljwx-offline-deployment-$(date +%Y%m%d).tar.gz \
    ljwx-images-*.tar.gz \
    ljwx-deployment/ \
    download-images.sh

echo "📦 离线部署包创建完成: ljwx-offline-deployment-$(date +%Y%m%d).tar.gz"
```

---

## 第二阶段：服务器离线部署（无网络环境）

### 1. 服务器环境准备

#### 1.1 系统要求
- **操作系统**: CentOS 7+, Ubuntu 18.04+, RHEL 7+
- **CPU**: 4核心以上
- **内存**: 8GB以上
- **硬盘**: 20GB以上可用空间
- **Docker**: 18.06+
- **Docker Compose**: 1.22+

#### 1.2 CentOS环境安装
```bash
# 安装Docker(离线环境需要提前准备RPM包)
sudo yum install -y docker

# 或者使用预编译二进制文件
curl -fsSL https://download.docker.com/linux/static/stable/x86_64/docker-20.10.21.tgz | \
tar xz && sudo cp docker/* /usr/bin/ && sudo dockerd &

# 安装Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/download/1.29.2/docker-compose-$(uname -s)-$(uname -m)" \
-o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 启动Docker服务
sudo systemctl start docker
sudo systemctl enable docker
```

### 2. 部署包传输

#### 2.1 文件传输方式
```bash
# 方式1: SCP传输
scp ljwx-offline-deployment-*.tar.gz user@server:/tmp/

# 方式2: U盘拷贝
# 将文件拷贝到U盘，在服务器插入U盘
mount /dev/sdb1 /mnt/usb
cp /mnt/usb/ljwx-offline-deployment-*.tar.gz /opt/

# 方式3: 内网文件服务器
# 通过内网FTP或HTTP服务器传输
```

#### 2.2 解压部署包
```bash
# 解压到部署目录
sudo mkdir -p /opt/ljwx
cd /opt/ljwx
sudo tar -xzf /tmp/ljwx-offline-deployment-*.tar.gz
cd ljwx-deployment
```

### 3. 导入Docker镜像

#### 3.1 导入镜像
```bash
# 解压镜像文件
gunzip ljwx-images-*.tar.gz

# 导入镜像到Docker
sudo docker load -i ljwx-images-*.tar

# 验证镜像导入
sudo docker images | grep ljwx
```

#### 3.2 镜像标签处理
```bash
# 如果需要重新标记镜像
cat > retag-images.sh << 'EOF'
#!/bin/bash
# 镜像重新标记脚本

REGISTRY="crpi-yilnm6upy4pmbp67.cn-shenzhen.personal.cr.aliyuncs.com"
VERSION="1.2.14"

# 重新标记镜像(可选)
docker tag $REGISTRY/ljwx/ljwx-mysql:$VERSION ljwx-mysql:$VERSION
docker tag $REGISTRY/ljwx/ljwx-redis:1.1.7 ljwx-redis:1.1.7
docker tag $REGISTRY/ljwx/ljwx-boot:$VERSION ljwx-boot:$VERSION
docker tag $REGISTRY/ljwx/ljwx-bigscreen:$VERSION ljwx-bigscreen:$VERSION
docker tag $REGISTRY/ljwx/ljwx-admin:$VERSION ljwx-admin:$VERSION

echo "✅ 镜像标记完成"
EOF

chmod +x retag-images.sh
sudo ./retag-images.sh
```

### 4. 配置部署环境

#### 4.1 配置文件定制
```bash
# 备份原配置
cp custom-config.env custom-config.env.bak

# 编辑配置文件
vim custom-config.env
```

```env
# 自定义配置示例
BIGSCREEN_TITLE=客户定制大屏标题
COMPANY_NAME=客户公司名称
SERVER_IP=192.168.1.100
BIGSCREEN_PORT=8001
ADMIN_PORT=8080
MYSQL_PASSWORD=custom_password
MYSQL_DATABASE=client_db
```

#### 4.2 CentOS兼容性检查
```bash
# 运行兼容性检查
chmod +x validate-centos-compatibility.sh
sudo ./validate-centos-compatibility.sh

# 修复权限
chmod +x *.sh
sudo chown -R $(whoami):$(whoami) .
```

### 5. 系统部署

#### 5.1 执行部署
```bash
# 设置执行权限
chmod +x deploy-client.sh

# 执行部署(CentOS环境使用bash)
sudo bash deploy-client.sh

# 或者分步执行
sudo docker-compose up -d mysql redis    #先启动基础服务
sleep 30                                  #等待服务就绪
sudo docker-compose up -d ljwx-boot      #启动后端服务
sleep 60                                  #等待服务就绪
sudo docker-compose up -d ljwx-bigscreen ljwx-admin  #启动前端服务
```

#### 5.2 部署状态检查
```bash
# 检查容器状态
sudo docker-compose ps

# 检查服务健康状态
sudo docker-compose exec ljwx-boot curl -f http://localhost:9998/actuator/health
sudo docker-compose exec ljwx-bigscreen curl -f http://localhost:8001/health

# 查看容器日志
sudo docker-compose logs -f ljwx-boot
sudo docker-compose logs -f ljwx-bigscreen
```

### 6. 防火墙和网络配置

#### 6.1 CentOS防火墙设置
```bash
# 开放必要端口
sudo firewall-cmd --permanent --add-port=8080/tcp  #管理后台
sudo firewall-cmd --permanent --add-port=8001/tcp  #大屏显示
sudo firewall-cmd --permanent --add-port=9998/tcp  #API服务
sudo firewall-cmd --reload

# 检查端口状态
sudo firewall-cmd --list-ports
sudo netstat -tlnp | grep -E ':(8080|8001|9998)'
```

#### 6.2 SELinux配置
```bash
# 检查SELinux状态
getenforce

# 如果需要，设置为宽松模式
sudo setenforce 0
sudo sed -i 's/SELINUX=enforcing/SELINUX=permissive/' /etc/selinux/config
```

---

## 第三阶段：系统验证和运维

### 1. 功能验证

#### 1.1 基础连通性测试
```bash
# 测试服务响应
curl -I http://localhost:8080/         #管理后台
curl -I http://localhost:8001/         #大屏显示
curl -I http://localhost:9998/actuator/health  #API健康检查

# 数据库连接测试
sudo docker-compose exec mysql mysql -uroot -p123456 -e "show databases;"
sudo docker-compose exec redis redis-cli ping
```

#### 1.2 Web界面访问
```bash
# 获取服务器IP地址
ip addr show | grep "inet " | grep -v 127.0.0.1

# 浏览器访问测试
echo "管理后台: http://服务器IP:8080"
echo "大屏显示: http://服务器IP:8001"
echo "API文档: http://服务器IP:9998/doc.html"
```

### 2. 数据备份配置

#### 2.1 自动备份设置
```bash
# 创建备份脚本
cat > /opt/ljwx/auto-backup.sh << 'EOF'
#!/bin/bash
# LJWX自动备份脚本

BACKUP_DIR="/opt/ljwx/backup"
DATE=$(date +%Y%m%d_%H%M%S)

# 创建备份目录
mkdir -p $BACKUP_DIR/mysql $BACKUP_DIR/redis

# MySQL备份
docker-compose exec -T mysql mysqldump -uroot -p123456 --all-databases > \
    $BACKUP_DIR/mysql/ljwx_mysql_$DATE.sql

# Redis备份
docker-compose exec -T redis redis-cli BGSAVE
docker cp ljwx-redis:/data/dump.rdb $BACKUP_DIR/redis/ljwx_redis_$DATE.rdb

# 清理7天前的备份
find $BACKUP_DIR -name "*.sql" -mtime +7 -delete
find $BACKUP_DIR -name "*.rdb" -mtime +7 -delete

echo "✅ 备份完成: $DATE"
EOF

chmod +x /opt/ljwx/auto-backup.sh

# 添加定时任务
echo "0 2 * * * /opt/ljwx/auto-backup.sh" | sudo crontab -
```

### 3. 监控和日志

#### 3.1 服务监控脚本
```bash
cat > /opt/ljwx/health-check.sh << 'EOF'
#!/bin/bash
# LJWX健康检查脚本

check_service() {
    local service=$1
    local port=$2
    if curl -sf http://localhost:$port/health > /dev/null 2>&1; then
        echo "✅ $service: 正常"
    else
        echo "❌ $service: 异常"
        docker-compose restart $service
    fi
}

echo "🔍 LJWX系统健康检查 - $(date)"
check_service ljwx-boot 9998
check_service ljwx-bigscreen 8001
check_service ljwx-admin 80

# 检查容器状态
echo "📊 容器状态:"
docker-compose ps
EOF

chmod +x /opt/ljwx/health-check.sh

# 添加监控定时任务
echo "*/5 * * * * /opt/ljwx/health-check.sh" | sudo crontab -
```

#### 3.2 日志管理
```bash
# 日志清理脚本
cat > /opt/ljwx/log-cleanup.sh << 'EOF'
#!/bin/bash
# 清理Docker日志

# 清理容器日志(保留最近7天)
docker system prune -f --filter "until=168h"

# 清理应用日志
find /opt/ljwx -name "*.log" -mtime +7 -delete

echo "🧹 日志清理完成: $(date)"
EOF

chmod +x /opt/ljwx/log-cleanup.sh
echo "0 3 * * 0 /opt/ljwx/log-cleanup.sh" | sudo crontab -
```

---

## 常见问题排除

### 1. 镜像相关问题

#### 1.1 镜像下载失败
```bash
# 重试下载特定镜像
docker pull crpi-yilnm6upy4pmbp67.cn-shenzhen.personal.cr.aliyuncs.com/ljwx/ljwx-mysql:1.2.14

# 检查网络连接
ping registry.docker.io
curl -I https://docker.io

# 配置Docker镜像源(国内环境)
sudo mkdir -p /etc/docker
cat > /etc/docker/daemon.json << 'EOF'
{
  "registry-mirrors": [
    "https://mirror.ccs.tencentyun.com",
    "https://registry.docker-cn.com"
  ]
}
EOF
sudo systemctl restart docker
```

#### 1.2 镜像导入失败
```bash
# 检查文件完整性
md5sum ljwx-images-*.tar.gz

# 检查Docker磁盘空间
docker system df
docker system prune -f

# 强制导入
docker load --input ljwx-images-*.tar
```

### 2. 部署相关问题

#### 2.1 容器启动失败
```bash
# 查看详细错误信息
docker-compose logs ljwx-boot
docker-compose logs ljwx-bigscreen

# 检查端口占用
netstat -tlnp | grep -E ':(3306|6379|8080|8001|9998)'

# 手动启动容器调试
docker run -it --rm ljwx-mysql:1.2.14 bash
```

#### 2.2 网络连接问题
```bash
# 检查Docker网络
docker network ls
docker network inspect ljwx-network

# 测试容器间连通性
docker-compose exec ljwx-boot ping mysql
docker-compose exec ljwx-bigscreen ping redis

# 重建网络
docker-compose down
docker-compose up -d
```

### 3. 性能优化

#### 3.1 系统优化
```bash
# 增加文件描述符限制
echo "* soft nofile 65536" >> /etc/security/limits.conf
echo "* hard nofile 65536" >> /etc/security/limits.conf

# 优化内核参数
echo "vm.max_map_count=262144" >> /etc/sysctl.conf
sysctl -p
```

#### 3.2 Docker优化
```bash
# Docker守护进程优化
cat > /etc/docker/daemon.json << 'EOF'
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "storage-driver": "overlay2"
}
EOF
sudo systemctl restart docker
```

---

## 安全建议

### 1. 访问控制
```bash
# 修改默认密码
vim custom-config.env  #修改MYSQL_PASSWORD等

# 配置防火墙白名单
firewall-cmd --permanent --add-rich-rule="rule family='ipv4' source address='192.168.1.0/24' accept"

# 禁用不必要的端口公网访问
firewall-cmd --permanent --remove-port=3306/tcp  #数据库端口
firewall-cmd --permanent --remove-port=6379/tcp  #Redis端口
```

### 2. 数据安全
```bash
# 启用数据库SSL
# 配置Redis认证
# 定期更新镜像版本
# 实施访问日志审计
```

---

## 技术支持

### 联系方式
- **技术支持**: support@ljwx.com
- **文档地址**: https://docs.ljwx.com
- **问题反馈**: https://github.com/ljwx/issues

### 版本信息
- **当前版本**: 1.2.14
- **支持系统**: CentOS 7+, Ubuntu 18.04+, RHEL 7+
- **Docker版本**: 18.06+
- **更新周期**: 每月定期更新

---

*© 2024 灵境万象科技. 保留所有权利.* 