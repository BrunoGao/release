name: Build and Push ljwx-boot Docker Image

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

env:
  REGISTRY: crpi-yilnm6upy4pmbp67.cn-shenzhen.personal.cr.aliyuncs.com/ljwx
  IMAGE_NAME: ljwx-boot
  VERSION: 1.1.2

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout ljwx-boot代码
      uses: actions/checkout@v4
      with:
        path: ljwx-boot
        
    - name: Checkout ljwx-boot-starter依赖
      uses: actions/checkout@v4
      with:
        repository: BrunoGao/ljwx-boot-starter
        token: ${{ secrets.GITHUB_TOKEN }}
        path: ljwx-boot-starter
        
    - name: 设置Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: 登录阿里云镜像仓库
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ secrets.ALIYUN_USERNAME }}
        password: ${{ secrets.ALIYUN_PASSWORD }}
        
    - name: 构建并推送Docker镜像
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ljwx-boot/ljwx-boot-admin/Dockerfile.prod
        platforms: linux/amd64
        push: true
        tags: |
          ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}
          ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
        
    - name: 验证镜像
      run: |
        echo "✅ ljwx-boot镜像构建并推送成功!"
        echo "🐳 镜像地址: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}"
        echo "📦 拉取命令: docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}"