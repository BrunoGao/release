# LJWX 灵境万象健康管理系统 - 客户端部署

## 🚀 快速开始

### 前置要求
- Docker 20.0+
- Docker Compose 2.0+
- Linux/macOS 系统
- 2GB+ 可用内存
- 10GB+ 可用磁盘空间

### 一键启动
```bash
# 下载并解压部署包到服务器
wget [部署包下载地址] -O ljwx-client.tar.gz
tar -xzf ljwx-client.tar.gz
cd ljwx-client

# 启动系统
./start.sh
```

### 系统访问
启动完成后，通过以下地址访问：

- **管理后台**: http://服务器IP
- **大屏系统**: http://服务器IP:5000  
- **后端API**: http://服务器IP:9998

默认账户：
- 用户名: `admin`
- 密码: `admin123`

## 📋 管理命令

```bash
# 查看服务状态
./start.sh status

# 停止服务
./start.sh stop

# 重启服务
./start.sh restart

# 查看所有日志
./start.sh logs

# 查看指定服务日志
./start.sh logs ljwx-boot

# 清理系统（慎用）
./start.sh clean

# 显示帮助
./start.sh help
```

## 🔧 配置说明

### 端口配置
编辑 `.env` 文件修改端口：
```bash
LJWX_ADMIN_PORT=80        # 前端管理系统
LJWX_BIGSCREEN_PORT=5000  # 大屏系统
LJWX_BOOT_PORT=9998       # 后端API
MYSQL_PORT=3306           # 数据库
REDIS_PORT=6379           # 缓存
```

### 数据库配置
编辑 `.env` 文件修改数据库设置：
```bash
MYSQL_ROOT_PASSWORD=123456
MYSQL_DATABASE=test
MYSQL_USER=ljwx
MYSQL_PASSWORD=123456
```

## 📂 目录结构

```
ljwx-client/
├── start.sh              # 一键启动脚本
├── docker-compose.yml    # Docker编排文件
├── .env                  # 环境变量配置
├── README.md             # 说明文档
├── data/                 # 数据持久化
│   ├── mysql/           # 数据库数据
│   └── redis/           # 缓存数据
├── config/               # 配置文件
│   ├── mysql/           # 数据库配置
│   └── redis/           # 缓存配置
├── logs/                 # 日志文件
│   ├── ljwx-boot/       # 后端日志
│   ├── ljwx-bigscreen/  # 大屏日志
│   └── nginx/           # 前端日志
└── backups/              # 备份文件
```

## 🔍 故障排除

### 端口占用
```bash
# 检查端口占用
sudo netstat -tlnp | grep :80
sudo netstat -tlnp | grep :5000

# 修改 .env 文件中的端口配置
```

### 服务无法启动
```bash
# 查看服务状态
./start.sh status

# 查看详细日志
./start.sh logs

# 重启服务
./start.sh restart
```

### 数据库连接失败
```bash
# 检查数据库服务
docker logs ljwx-mysql

# 进入数据库容器
docker exec -it ljwx-mysql mysql -u root -p
```

### 清理重新开始
```bash
# 停止并清理所有数据
./start.sh clean

# 重新启动
./start.sh start
```

## 📞 技术支持

如遇问题，请联系技术支持：
- 📧 邮箱: support@ljwx.com
- 📱 电话: 400-XXX-XXXX
- 🔧 在线文档: https://docs.ljwx.com

## 📄 版本信息

- 系统版本: 2.0.1
- 更新日期: 2025-09-06
- 兼容性: Docker 20.0+, Linux/macOS