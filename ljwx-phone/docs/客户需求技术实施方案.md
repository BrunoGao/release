# 客户需求技术实施方案 📋

> 基于当前代码架构分析的四个客户需求落地方案，按实施难度和优先级排序

## 📊 需求概览与优先级

| 需求编号 | 功能描述 | 工期(人/天) | 实施难度 | 优先级 | 技术风险 |
|---------|----------|-------------|----------|--------|----------|
| **需求1** | Excel批量导入用户及设备绑定 | 10 | ⭐⭐ | 🔥🔥🔥 | 低 |
| **需求4** | 手机端管理员权限分级查看 | 15 | ⭐⭐ | 🔥🔥🔥 | 低 |
| **需求2** | 批量处理告警功能 | 5 | ⭐ | 🔥🔥 | 极低 |
| **需求3** | 告警通知系统重构 | 40 | ⭐⭐⭐⭐ | 🔥🔥🔥🔥 | 中 |

## 🎯 **需求1: Excel批量导入用户及设备绑定**

### 📈 实施难度: ⭐⭐ (简单)
### ⏱ 工期估算: 10人/天
### 🚨 技术风险: 低

### 现状分析
**现有基础设施** ✅
- 已有`EmployeeController`支持员工管理
- 已有`SysUserController`和`SysOrgUnitsController`支持用户和组织管理
- 已有`TDeviceUserController`支持设备用户绑定

### 技术实施方案

#### 1.1 后端实现 (6人/天)

**新增Controller接口**:
```java
@RestController
@RequestMapping("/import")
public class ImportController {
    
    @PostMapping("/users/excel")
    @SaCheckPermission("import:users:excel")
    public Result<ImportResultVO> importUsersFromExcel(
        @RequestParam("file") MultipartFile file) {
        // Excel解析和批量导入逻辑
    }
    
    @GetMapping("/template/download")
    public ResponseEntity<Resource> downloadTemplate() {
        // 下载Excel导入模板
    }
}
```

**核心服务实现**:
```java
@Service
public class ExcelImportService {
    
    public ImportResultVO importUsersWithDevices(MultipartFile file) {
        // 1. 验证Excel格式和字段
        // 2. 验证部门和岗位是否存在
        // 3. 批量创建用户
        // 4. 自动绑定设备信息
        // 5. 返回导入结果统计
    }
    
    private void validateDepartmentAndPosition(String deptName, String positionName) {
        // 部门和岗位名称唯一性验证
    }
}
```

#### 1.2 前端实现 (3人/天)

**Excel导入页面**:
```typescript
// 新增导入页面组件
const ImportUsersPage = () => {
  return (
    <div>
      <Upload
        accept=".xlsx,.xls"
        beforeUpload={validateExcelFile}
        customRequest={handleExcelUpload}
      >
        <Button icon={<UploadOutlined />}>
          批量导入用户Excel
        </Button>
      </Upload>
      <ImportResultTable data={importResult} />
    </div>
  );
};
```

#### 1.3 Excel模板设计
```
| 姓名 | 手机号 | 部门名称 | 岗位名称 | 设备序列号 | 备注 |
|------|--------|----------|----------|------------|------|
| 张三 | 13800138000 | 技术部 | 工程师 | DEVICE001 | 测试 |
```

### 实施计划
1. **Day 1-2**: Excel解析库集成和模板设计
2. **Day 3-5**: 后端批量导入逻辑开发
3. **Day 6-7**: 前端导入界面开发
4. **Day 8-9**: 联调测试和异常处理
5. **Day 10**: 文档编写和部署准备

---

## 🎯 **需求4: 手机端管理员权限分级查看**

### 📈 实施难度: ⭐⭐ (简单)
### ⏱ 工期估算: 15人/天
### 🚨 技术风险: 低

### 现状分析
**现有基础设施** ✅
- 已有用户角色权限系统(`SysUserRoleController`)
- 已有组织架构管理(`SysOrgUnitsController`)
- Flutter手机端已有基础架构

### 技术实施方案

#### 4.1 权限系统扩展 (5人/天)

**用户类型枚举**:
```java
public enum UserType {
    EMPLOYEE("普通员工"),
    MANAGER("部门管理员"),
    ADMIN("系统管理员");
}
```

**权限验证服务**:
```java
@Service
public class UserPermissionService {
    
    public boolean canViewUserData(String currentUserId, String targetUserId) {
        // 管理员可查看部门内所有员工数据
        // 普通员工只能查看自己的数据
    }
    
    public List<SysUserVO> getAccessibleUsers(String userId) {
        // 根据用户权限返回可访问的用户列表
    }
}
```

#### 4.2 手机端路由改造 (6人/天)

**Flutter路由逻辑**:
```dart
// 登录后自动路由判断
class AuthService {
  Future<void> handleLoginSuccess(UserModel user) async {
    if (user.userType == UserType.MANAGER || user.userType == UserType.ADMIN) {
      // 跳转到管理员主页
      GoRouter.of(context).go('/admin/dashboard');
    } else {
      // 跳转到员工个人页面
      GoRouter.of(context).go('/employee/personal');
    }
  }
}

// 管理员页面组件
class AdminDashboardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          DepartmentUserList(), // 部门员工列表
          HealthDataOverview(), // 健康数据概览
        ],
      ),
    );
  }
}
```

#### 4.3 数据权限控制 (4人/天)

**API接口改造**:
```java
@GetMapping("/health/department")
@SaCheckPermission("health:department:view")
public Result<List<HealthDataVO>> getDepartmentHealthData(
    @RequestParam String deptId) {
    // 验证当前用户是否有查看该部门数据的权限
    return Result.data(healthDataService.getDepartmentData(deptId));
}
```

### 实施计划
1. **Day 1-3**: 后端权限系统扩展
2. **Day 4-8**: Flutter路由和页面改造
3. **Day 9-12**: 数据权限控制API开发
4. **Day 13-14**: 联调测试
5. **Day 15**: 部署和文档

---

## 🎯 **需求2: 批量处理告警功能**

### 📈 实施难度: ⭐ (极简单)
### ⏱ 工期估算: 5人/天
### 🚨 技术风险: 极低

### 现状分析
**现有基础设施** ✅
- 已有`TAlertInfoController`告警管理
- 已有批量删除功能(`batchDelete`)
- 前端已有选择多个记录的UI组件

### 技术实施方案

#### 2.1 后端接口扩展 (2人/天)

**批量处理接口**:
```java
@RestController
@RequestMapping("t_alert_info")
public class TAlertInfoController {
    
    @PostMapping("/batch/process")
    @SaCheckPermission("t:alert:info:batch:process")
    public Result<Boolean> batchProcessAlerts(
        @RequestBody BatchProcessAlertDTO batchProcessDTO) {
        return Result.status(tAlertInfoFacade.batchProcess(batchProcessDTO));
    }
}

@Data
public class BatchProcessAlertDTO {
    private List<Long> alertIds;
    private String processAction; // "resolve", "ignore", "escalate"
    private String processComment;
    private String processorId;
}
```

#### 2.2 消息推送集成 (2人/天)

**消息推送服务**:
```java
@Service
public class AlertNotificationService {
    
    public void sendBatchProcessNotification(List<Long> alertIds, String action) {
        // 发送批量处理结果到个人和管理员
        alertIds.forEach(alertId -> {
            TAlertInfo alert = alertService.getById(alertId);
            // 推送给相关用户和管理员
            messageService.sendToUser(alert.getUserId(), buildNotification(alert, action));
            messageService.sendToAdmins(buildAdminNotification(alert, action));
        });
    }
}
```

#### 2.3 前端UI改造 (1人/天)

**批量操作组件**:
```typescript
const AlertBatchActions = ({ selectedAlerts }) => {
  return (
    <Space>
      <Button 
        type="primary"
        disabled={selectedAlerts.length === 0}
        onClick={() => handleBatchProcess('resolve')}
      >
        批量处理 ({selectedAlerts.length})
      </Button>
      <Button onClick={() => handleBatchProcess('ignore')}>
        批量忽略
      </Button>
    </Space>
  );
};
```

### 实施计划
1. **Day 1**: 后端批量处理接口开发
2. **Day 2**: 消息推送逻辑集成
3. **Day 3**: 前端UI组件开发
4. **Day 4**: 联调测试
5. **Day 5**: 部署上线

---

## 🎯 **需求3: 告警通知系统重构**

### 📈 实施难度: ⭐⭐⭐⭐ (复杂)
### ⏱ 工期估算: 40人/天
### 🚨 技术风险: 中

### 现状分析
**现有基础设施** ✅
- 已有`TAlertRulesController`告警规则管理
- 已有`TWechatAlertConfigController`微信告警配置
- 已有设备消息系统(`TDeviceMessageController`)
- ljwx-bigscreen已有告警处理逻辑

### 技术实施方案

#### 3.1 告警规则系统重构 (12人/天)

**新的通知方式配置**:
```java
@Entity
public class TAlertRules {
    // 原有字段...
    
    @Enumerated(EnumType.STRING)
    private NotificationType notificationType; // APP_MESSAGE, WECHAT, SMS, EMAIL
    
    @Column(columnDefinition = "JSON")
    private String notificationConfig; // JSON配置不同通知方式的参数
    
    private Boolean enableAutoProcess; // 是否启用自动处理
    private String autoProcessRules; // 自动处理规则JSON
}

public enum NotificationType {
    APP_MESSAGE("应用内消息"),
    DEVICE_MESSAGE("设备消息"),
    WECHAT("微信通知"),
    SMS("短信通知"),
    BIGSCREEN_POPUP("大屏弹窗");
}
```

**智能告警匹配引擎**:
```java
@Service
public class AlertMatchingEngine {
    
    public AlertProcessResult processSystemEvent(SystemEventDTO event) {
        // 1. 根据事件类型、严重程度、设备信息匹配告警规则
        List<TAlertRules> matchedRules = findMatchingRules(event);
        
        if (!matchedRules.isEmpty()) {
            // 2. 自动创建告警记录
            TAlertInfo alert = createAlertFromEvent(event, matchedRules.get(0));
            
            // 3. 根据规则配置发送通知
            sendNotificationsByRules(alert, matchedRules);
            
            return AlertProcessResult.builder()
                .processed(true)
                .alertId(alert.getId())
                .matchedRules(matchedRules.size())
                .build();
        }
        
        // 无法匹配的告警需要人工处理
        return AlertProcessResult.builder()
            .processed(false)
            .requiresManualReview(true)
            .build();
    }
}
```

#### 3.2 多渠道通知系统 (15人/天)

**统一消息推送服务**:
```java
@Service
public class UnifiedNotificationService {
    
    public void sendAlert(TAlertInfo alert, NotificationType type) {
        switch (type) {
            case APP_MESSAGE:
                sendToMobileApp(alert);
                break;
            case DEVICE_MESSAGE:
                sendToWatchDevice(alert);
                break;
            case BIGSCREEN_POPUP:
                sendToBigScreen(alert);
                break;
            case WECHAT:
                sendToWechat(alert);
                break;
        }
    }
    
    private void sendToMobileApp(TAlertInfo alert) {
        // 推送到手机APP
        AppPushMessage message = AppPushMessage.builder()
            .userId(alert.getUserId())
            .title("健康告警")
            .content(alert.getAlertContent())
            .type("HEALTH_ALERT")
            .data(Map.of("alertId", alert.getId()))
            .build();
            
        appPushService.send(message);
    }
    
    private void sendToWatchDevice(TAlertInfo alert) {
        // 推送到手表设备
        DeviceMessage deviceMsg = DeviceMessage.builder()
            .deviceId(alert.getDeviceId())
            .messageType("ALERT")
            .content(alert.getAlertContent())
            .priority(alert.getAlertLevel())
            .build();
            
        deviceMessageService.send(deviceMsg);
    }
    
    private void sendToBigScreen(TAlertInfo alert) {
        // 推送到大屏弹窗 (仅限critical级别)
        if ("critical".equals(alert.getAlertLevel())) {
            BigScreenPopupMessage popup = BigScreenPopupMessage.builder()
                .alertId(alert.getId())
                .title("紧急健康告警")
                .content(alert.getAlertContent())
                .userName(alert.getUserName())
                .alertTime(alert.getCreateTime())
                .autoClose(false) // 紧急告警需要手动关闭
                .build();
                
            bigScreenService.showPopup(popup);
        }
    }
}
```

#### 3.3 手机端告警响应系统 (8人/天)

**Flutter告警消息处理**:
```dart
class AlertNotificationService {
  
  // 定时拉取告警消息
  Timer? _alertPollingTimer;
  
  void startAlertPolling() {
    _alertPollingTimer = Timer.periodic(
      const Duration(seconds: 30),
      (_) => fetchAndDisplayAlerts(),
    );
  }
  
  Future<void> fetchAndDisplayAlerts() async {
    try {
      final alerts = await apiService.getUserUnreadAlerts();
      
      for (final alert in alerts) {
        // 显示本地通知
        await showLocalNotification(alert);
        
        // 如果是critical级别，显示全屏告警
        if (alert.level == AlertLevel.critical) {
          await showFullScreenAlert(alert);
        }
      }
    } catch (e) {
      print('获取告警消息失败: $e');
    }
  }
  
  Future<void> handleAlertResponse(String alertId, AlertResponse response) async {
    // 处理用户对告警的响应
    await apiService.respondToAlert(alertId, response);
    
    // 更新本地告警状态
    await updateLocalAlertStatus(alertId, response);
  }
}

// 告警响应页面
class AlertResponseScreen extends StatelessWidget {
  final AlertModel alert;
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('告警处理')),
      body: Column(
        children: [
          AlertDetailCard(alert: alert),
          AlertResponseActions(
            onConfirm: () => handleResponse(AlertResponse.confirmed),
            onIgnore: () => handleResponse(AlertResponse.ignored),
            onEscalate: () => handleResponse(AlertResponse.escalated),
          ),
        ],
      ),
    );
  }
}
```

#### 3.4 管理端告警日志系统 (5人/天)

**告警处理日志接口**:
```java
@RestController
@RequestMapping("/alert/logs")
public class AlertLogController {
    
    @GetMapping("/processing")
    @SaCheckPermission("alert:logs:processing:view")
    public Result<RPage<AlertProcessLogVO>> getProcessingLogs(
        @Parameter PageQuery pageQuery,
        @Parameter AlertLogSearchDTO searchDTO) {
        return Result.data(alertLogService.getProcessingLogs(pageQuery, searchDTO));
    }
    
    @GetMapping("/response")
    @SaCheckPermission("alert:logs:response:view")
    public Result<RPage<AlertResponseLogVO>> getResponseLogs(
        @Parameter PageQuery pageQuery,
        @Parameter AlertLogSearchDTO searchDTO) {
        return Result.data(alertLogService.getResponseLogs(pageQuery, searchDTO));
    }
}
```

### 实施计划
1. **Day 1-5**: 告警规则系统重构和自动匹配引擎
2. **Day 6-15**: 多渠道通知系统开发
3. **Day 16-25**: 手机端告警响应系统
4. **Day 26-30**: 管理端日志系统
5. **Day 31-35**: 大屏弹窗集成
6. **Day 36-38**: 系统联调测试
7. **Day 39-40**: 部署和文档

---

## 📋 总体实施建议

### 🔥 推荐实施优先级

1. **第一期 (最高优先级)**: 
   - ✅ 需求2: 批量处理告警 (5天)
   - ✅ 需求1: Excel批量导入 (10天)
   - **总计: 15天**

2. **第二期 (高优先级)**:
   - ✅ 需求4: 手机端管理员权限 (15天)

3. **第三期 (战略性功能)**:
   - ✅ 需求3: 告警通知系统重构 (40天)

### 🛠 技术风险控制

1. **需求1&2&4**: 风险极低，基于现有架构扩展
2. **需求3**: 中等风险，需要系统性重构
   - 建议分阶段实施
   - 保持向后兼容
   - 充分测试告警规则匹配逻辑

### 📊 资源分配建议

- **后端开发**: 60% (Java Spring Boot)
- **前端开发**: 25% (Vue3 + Flutter)  
- **测试联调**: 10%
- **部署文档**: 5%

### 🎯 成功标准

1. **功能完整性**: 所有需求功能100%实现
2. **性能指标**: 告警响应时间<3秒，批量处理支持1000+记录
3. **用户体验**: 手机端响应流畅，管理界面直观易用
4. **系统稳定性**: 7×24小时稳定运行，告警不丢失

---

*文档版本: v1.0*  
*创建时间: 2025-08-19*  
*负责人: ljwx技术团队*