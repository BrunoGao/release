import 'package:flutter/material.dart';
import '../theme/app_theme.dart';

// 使用主题文件中定义的颜色
const kPrimaryColor = AppTheme.primaryColor;
const kBgGradient = LinearGradient(colors: [Color(0xFF15202B), Color(0xFF1E2839)]); // 深蓝灰渐变
const kCardGradient = LinearGradient(colors: [Color(0xFF1E2839), Color(0xFF263545)]);
const kLogoAsset = 'assets/logo.png'; // 企业logo
